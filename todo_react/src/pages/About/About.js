// CSS
import styles from "./About.module.css"

const About = () => {
  return (
    <div className={styles.about}>
      <h2>About Task Manager</h2>
      <p>This project consists of a task manager built in React + Typescript on the front-end and Firebase on the back-end.</p>
    </div>
  )
}

export default About